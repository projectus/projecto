<% if @collaboration_application.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@collaboration_application.errors.count, "error") %> prohibited this collaboration_application from being saved:</h2>

    <ul>
    <% @collaboration_application.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_for(@collaboration_application) do |f| %>
  <%= f.hidden_field :status, value: CollaborationApplication::STATUS_ACCEPTED %>    

  <div class="actions">
    <%= f.submit 'Accept' %>
  </div>
  
<% end %>

<%= form_for(@collaboration_application) do |f| %>
  <%= f.hidden_field :status, value: CollaborationApplication::STATUS_DECLINED %>    

  <div class="actions">
    <%= f.submit 'Decline' %>
  </div>
  
<% end %>