<p>
  <%= type.to_s.split('_').map(&:capitalize).join(' ') %>:
  <% if type == :birthday %>
    <%= entry.split(',').join('-') %>
  <% elsif type == :name %>
    <%= entry.split(',').join(' ') %>
  <% else %>
    <%= entry %>
  <% end %>
</p>
