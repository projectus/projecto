<fieldset>
  <h5>Contact Card</h5>
  <div class="well well-small">
    <% contact.each do |type,entry| %>
      <%= render partial: 'contact_entry', object: entry, locals: {type: type} %>
    <% end %>
  </div>
</fieldset>