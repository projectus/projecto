<%= form_for(task) do |f| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div style="display: inline-block;">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div style="display: inline-block;">
    <%= f.label :details %>
    <%= f.text_area :details %>
  </div>

  <div style="display: inline-block;">
    <%= f.label :priority %>
    <%= f.number_field :priority %>
  </div>

  <%= f.hidden_field :task_group_id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
