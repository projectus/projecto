<table class="table table-striped table-bordered box">
  <thead>
    <tr>
      <th>Status</th>
      <th>User</th>
      <th>Invited by</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @project.invitations.includes({invited_user: :profile,invited_by_user: :profile}).each do |invitation| %>
      <tr>
        <td><%= invitation.status %></td>
        <td><%= link_to invitation.invited_user.username, invitation.invited_user.profile %></td>
        <td><%= link_to invitation.invited_by_user.username, invitation.invited_by_user.profile %></td>
        <td><%= link_to 'Details', invitation %></td>
      </tr>
    <% end %>
  </tbody>
</table>
