<%= form_for(@comment) do |f| %>
  <%= render 'errors' %>
  <div class="field">
    <%= f.text_area :content %>
  </div>

  <div class="field">
    <%= f.hidden_field :project_id %>
  </div>

  <div class="actions">
    <%= f.submit 'Post Comment', class: 'btn' %>
  </div>
<% end %>

