<% if @collaboration.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@collaboration.errors.count, "error") %> prohibited this application from being accepted:</h2>

    <ul>
    <% @collaboration.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
