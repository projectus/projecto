
<%= link_to 'Edit Details', edit_project_profile_path(@project_profile), class: 'btn btn-small pull-right' %>

<% @details.sort.map do |key,entry| %>
  <%= render partial: 'project_details_entry/entry', locals: {entry: entry} %>
<% end %>

